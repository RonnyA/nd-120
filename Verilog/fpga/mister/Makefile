# ND-120 on MiSTer (DE10-Nano) - standard board-folder build API (see ../README.md)
#
# The MiSTer port is still in the planning phase - no Quartus project exists
# yet. These targets are placeholders so every board folder answers to the
# same commands; they will delegate to the Docker-based Quartus 17.0.2 flow
# (docs/02-building.md) once the core skeleton lands.

all load flash:
	@echo "mister: no build yet - the port is in the planning phase."
	@echo "See docs/00-overview.md (phase plan) and docs/02-building.md (intended Quartus-in-Docker flow)."
	@exit 1

.PHONY: all load flash
